# Shared test-only defaults, layered under every test's own @TestPropertySource.
# GATEWAY_CORS_ALLOWED_ORIGINS has no wildcard default in main config (fail-closed
# by design) so tests must supply a value or context startup fails.
GATEWAY_CORS_ALLOWED_ORIGINS=http://localhost:5173
